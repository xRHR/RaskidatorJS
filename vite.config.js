import { defineConfig } from 'vite';

// Set the `base` to the repository name so GitHub Pages serves assets correctly
// when the site is hosted at https://<owner>.github.io/<repo>/
export default defineConfig({
  base: '/RaskidatorJS/'
});
